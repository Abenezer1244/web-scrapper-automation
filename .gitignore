# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
.venv-schema/   # throwaway pinned env for regenerating schema/openapi.json

# Environment variables (all variants; keep the documented example tracked)
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Data and logs
data/exports/*
!data/exports/.gitkeep
logs/*
!logs/.gitkeep

# Testing
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db

# Terraform
infra/terraform/.terraform/
infra/terraform/terraform.tfvars
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/.terraform.lock.hcl

# Claude worktrees & skills
.claude/worktrees/
.claude/skills/gstack/
# Per-machine local settings file. Do NOT commit — it carries an
# ANTHROPIC_API_KEY plus per-MCP bearer tokens by default.
.claude/settings.local.json
# Per-machine settings: wires claude-flow hooks to local-only helper scripts
# (.claude/helpers/*.cjs|mjs) that are not tracked. Keep local to avoid
# dangling hook references in the repo.
.claude/settings.json
tests/screenshots/
.playwright-mcp/
.playwright-cli/
.playwright/
*.png

# AI tool configs (bulk-installed, not project files)
.adal/
.agent/
.agents/
.augment/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
.claude-flow/
.swarm/
skills/
skills-lock.json
design-system/

# Separate projects (not part of this repo)
aiwebscrapper/

# E2E test credentials (never commit) — broad glob so variant names
# (.e2e_king_creds.json, .e2e_pierce_live.json, ...) can't slip past.
scripts/.e2e_*.json
scripts/.e2e*.json

# Runtime artifacts
.ruff_cache/
.firecrawl/

# Terraform local state + provider cache (never commit — contains binaries/state)
**/.terraform/
*.tfstate
*.tfstate.*

# Scratch test outputs (exports, dumps, screenshots, logs)
scripts/*.csv
scripts/e2e_results.log
scripts/e2e_screenshots/
_diag_is_pro/
# Root-level scratch screenshots / diagnostic dumps (the ones cleaned 2026-06-01)
/e2e_*.png
/*arms*.png
/diag_*
/_diag_*

# RLS cutover role passwords (local operator secret — never commit)
.rls-cutover-secrets
.rls-cutover-repoint.sh

# graphify generated output (knowledge graph + cache; regenerated by hooks, has local abs paths)
graphify-out/
graphify-out-combined/
src/graphify-out/

# Local-only Node Playwright QA experiments (this is a Python repo; no package.json)
tests/live_qa_chelan.js
tests/live_qa_chelan.mjs
.gstack/
.codex_consult.txt
.codex_err.txt

# Root-level scraping/backfill scratch dumps (logs + verify .txt; screenshots already covered by *.png)
/*.log
/king_*
# Diagnostic audit output dirs (regenerated by live audit scripts)
scripts/audit_out/
scripts/audit_out_saas/
scripts/audit_out_ui/

# Local schema-regen venv (pinned deps for export_openapi.py)
.venv-schema/
