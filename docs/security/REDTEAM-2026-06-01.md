# Red-Team Audit — Claude × Codex — 2026-06-01

**Mission:** Two independent reviewers (Claude + Codex), each finds & *proves* exploits, then verifies the other's findings to the end. Fix derived from the exploit. Any Critical/High from either reviewer = NO-GO until fixed.

**Doctrine (codex-collaboration.md):** Both flag → higher severity. Only one flags → re-read; adopt if real. Disagreement → Codex wins unless CLAUDE.md/PRD says otherwise.

Status legend: 🔴 open · 🟡 in-progress · ✅ fixed+verified · ⬜ refuted/wontfix

---

## Round 1 — Claude's findings (proven, not yet Codex-verified)

### SSRF / Scraper navigation
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| S1 | CRITICAL | Playwright route guard early-returns `True` for non-`document` requests → in-page `fetch`/XHR reach `169.254.169.254` & internal hosts, fully outside `safe_http`. `base_scraper.py:186-204` | _pending_ | 🔴 |
| S2 | HIGH | AI navigator executes model-emitted `evaluate` JS verbatim; malicious county page prompt-injects → arbitrary in-page JS (chains into S1). `navigator.py:361-364`, `paginator.py:90-95` | _pending_ | 🔴 |
| S3 | MEDIUM | `gis_endpoint` validate-time vs fetch-time TOCTOU (latent; dormant arg). `scrapers.py:363-371` → `county_gis.py` | _pending_ | 🔴 |
| S4 | MEDIUM | ~10 raw `requests.get/post` bypass `safe_http` (redirect-follow + `trust_env=True`). `county_gis.py:241,519`, `national.py:57`, `king_county_assessor.py:53`, `skip_trace.py:309`, etc. | _pending_ | 🔴 |
| S5 | LOW | IDNA no-op `except: pass` + thin loopback-alias blocklist + `resolve=False` create-time default. `security.py:85-115` | _pending_ | 🔴 |

### Auth / Session
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| A1 | HIGH | Refresh tokens never rotated — stolen refresh token replayable 7d, self-renewing indefinitely; old token survives refresh. `routes/auth.py:170-216` | _pending_ | 🔴 |
| A2 | HIGH | `change_password` does not revoke sessions/refresh tokens — compromised account stays compromised after the "fix". `routes/auth.py:377-425` | _pending_ | 🔴 |
| A3 | HIGH | No forgot/reset-password flow → unrecoverable takeover when combined with A1/A2. (absent) | _pending_ | 🔴 |
| A4 | MEDIUM | `/register` timing enumeration — duplicate check returns before bcrypt; registered emails respond faster. `routes/auth.py:60-116` | _pending_ | 🔴 |
| A5 | MEDIUM | `/refresh` `except (InvalidTokenError, Exception)` catch-all flips fail-closed 503 → 401. `routes/auth.py:178-182` | _pending_ | 🔴 |
| A6 | LOW | Brute-force per-email counter → targeted 24h account-lockout DoS; fail-open on Redis error. `auth_hardening.py:286-355` | _pending_ | 🔴 |

### Multi-tenancy / IDOR / RLS
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| T1 | HIGH | `/download` branches authz on client-set `purpose` claim (`verify_aud=False`) → a session JWT is accepted as a download token, skipping the `job_id` binding. `routes/jobs.py:631-664` | _pending_ | 🔴 |
| T2 | HIGH | RLS not enforced — runtime role has BYPASSRLS; write policies lack `WITH CHECK`. `user_id` filter is the *only* tenant boundary. `db/session.py:153-208`, `001_initial.py:136-156` | _pending_ | 🔴 |
| T3 | MEDIUM | Tracerfy webhook `queue_id` forge/replay + body-supplied `download_url` → inject fabricated phone/email into a batch owner's rows. `webhooks.py:93-136` | _pending_ | 🔴 |
| T4 | LOW | `download_export` uses `get_db` (no RLS ctx) + manual `set_config`; fragile sole-guard. `routes/jobs.py:568,686` | _pending_ | 🔴 |

### Exports / CSV injection
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| E1 | CRITICAL | Formula-injection bypass via leading `"` (Excel/Sheets strip CSV quote, then evaluate `=`). `security.py:391` `sanitize_for_csv`. Verified: `'"=1+1"'` → not quoted. | _pending_ | 🔴 |
| E2 | CRITICAL | `.xlsx` export inherits the bypass and has no Excel-native cell neutralization. `data_exporter.py:129-153` | _pending_ | 🔴 |
| E3 | HIGH | Embedded TAB formula injection survives (`clean_text` keeps `\t`); splits to a new `=`-cell. `security.py:391,397` | _pending_ | 🔴 |
| E4 | HIGH | `to_json` only sanitizes `isinstance(v,str) and v` → non-str values unsanitized; double-quote bypass passes. `data_exporter.py:155-168` | _pending_ | 🔴 |
| E5 | MEDIUM | DNC disclaimer appended via second `open(...,'a')` after `to_csv` — write path outside the sanitizer. `data_exporter.py:121-124` | _pending_ | 🔴 |

### Billing / Webhooks
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| B1 | HIGH | Tracerfy webhook has no replay/idempotency guard → `skip_trace_used_this_month` inflated → victim billed overage on legit usage (also fires on Celery redelivery). `webhooks.py:46-138` → `tracerfy_ingest.py` → `skip_trace_usage.py:116-123` | _pending_ | 🔴 |
| B2 | HIGH | Counter advance committed before Stripe meter & decoupled (swallowed Stripe error) → counter/meter divergence, pumpable. `skip_trace_usage.py:76-136` | _pending_ | 🔴 |
| B3 | LOW | `invoice.payment_failed` `attempt_count` unbounded into email/log. `billing.py:785-800` | _pending_ | 🔴 |
| B4 | LOW | Public `/billing/pricing` does uncached `stripe.Coupon.retrieve` per hit + bare `except` → Stripe-rate amplification, blind. `billing.py:319-350` | _pending_ | 🔴 |

### Infra / Secrets / Middleware
| ID | Sev | Finding | Codex verdict | Status |
|----|-----|---------|---------------|--------|
| I1 | HIGH | Rate limit bypass — trusts **leftmost** `X-Forwarded-For` behind proxy; Railway appends, so leftmost is attacker-chosen → unlimited keys → `/auth/login` & webhook limits never trip. `rate_limit.py:65-79` | _pending_ | 🔴 |
| I2 | MEDIUM | Rate limiter fails **open** globally on any `RedisError` → auth brute-force window during Redis incident. `rate_limit.py:103-119` | _pending_ | 🔴 |
| I3 | MEDIUM | Homeowner PII (`party_name[:30]`) written to logs on enqueue failure; redaction filter only catches secret-shapes. `tasks.py:1039` | _pending_ | 🔴 |
| I4 | LOW | CORS would reflect `*` with credentials if `ALLOWED_ORIGINS` misconfigured; no validation. `main.py:54-65` | _pending_ | 🔴 |

---

## Verified hardened (attacked, held) — do not regress
SSRF `safe_http` wrapper (resolve+no-redirect+trust_env=False+same-origin pin) · webhook-delivery SSRF · Tracerfy CSV fetch via `safe_get_following` · Stripe webhook HMAC + raw-body + `SET NX` idempotency · forged checkout metadata.user_id (customer-id cross-check) · referral double-credit (`UNIQUE(referee_id)`) · JWT alg/aud/iss pinning · API-key entropy+hash lookup · `is_admin` from live DB row (not JWT) · Celery json-only (no pickle) · no subprocess/eval/exec · global error handler (ref id, no stack) · security headers · SECRET_KEY ≥32 validator.

---

## Round 2 — Codex cross-verification: COMPLETE

Codex (different model family) independently re-derived every finding from the code. Disagreements resolved in Codex's favour per doctrine. Final adjudicated register:

### MUST-FIX — HIGH (NO-GO until fixed)
| ID | Final Sev | Reconciled verdict |
|----|-----------|--------------------|
| S1 | HIGH | CONFIRM (was CRITICAL). In-page `fetch`/XHR bypass the document-only route guard → **blind** SSRF / internal port-scan / state-changing GET to internal hosts & `169.254.169.254`. Codex: response-read is CORS-blocked, so "readable metadata" over-claimed → HIGH not CRITICAL. Still must fix. |
| S2 | HIGH | CONFIRM. Model-emitted `evaluate` JS executed verbatim from attacker-controlled page (prompt-injection → arbitrary in-page JS; chains into S1). |
| A1 | HIGH | CONFIRM. Refresh token never rotated — stolen token replayable/self-renewing. |
| A2 | HIGH | CONFIRM. `change_password` doesn't revoke sessions. |
| T3 | HIGH | CONFIRM (Codex raised MED→HIGH). Tracerfy webhook trusts `queue_id`+body `download_url`; secret-holder/replay injects fabricated phone/email into a batch owner's rows. |
| E1 | HIGH | CONFIRM (was CRITICAL). `sanitize_for_csv` leading-`"` formula-injection bypass; CSV-importer-dependent → HIGH not CRITICAL. |
| B1 | HIGH | CONFIRM. Tracerfy webhook no idempotency → skip-trace counter inflated → victim billed overage (also fires on Celery redelivery). |
| I1 | HIGH | CONFIRM. Leftmost-XFF trust → rate-limit bypass on `/auth/login` + webhooks. |
| T2 | HIGH (verify) | UNSURE — needs live DB role check. Migrations lack `WITH CHECK`; if runtime role is BYPASSRLS the `user_id` filter is the ONLY tenant boundary. Action: verify role + add `WITH CHECK` + fail-closed startup. |

### SHOULD-FIX — MEDIUM
| ID | Final Sev | Reconciled verdict |
|----|-----------|--------------------|
| A3 | MEDIUM | PARTIAL (was HIGH). No password-reset flow — recovery gap; HIGH only after takeover. |
| A4 | MEDIUM | CONFIRM. `/register` timing enumeration (bcrypt only on new-email branch). |
| E3 | MEDIUM | PARTIAL (was HIGH). Embedded-TAB formula injection; spreadsheet-tab-handling dependent. |
| B2 | MEDIUM | CONFIRM (was HIGH). Counter commits before Stripe + swallowed error → counter/meter divergence. |
| I2 | MEDIUM | CONFIRM. Rate limiter fails open globally on RedisError. |
| I3 | MEDIUM | CONFIRM. Homeowner PII (`party_name`) in logs. |
| N1 | MEDIUM | **NEW (Codex).** PACS assessor SSRF — `is_pacs_url` only checks `propertyaccess`; DB `assessor_url` → raw `requests` follows redirects. `pacs.py:32,60,75`, `tasks.py:802,811,824`. |
| N2 | MEDIUM | **NEW (Codex).** Unauthenticated `/scrapers/sample` returns real `property_address`/`mailing_address` from live results. `scrapers.py:28,70-75`. (Confirm product intent.) |

### LOW / hygiene
| ID | Final Sev | Reconciled verdict |
|----|-----------|--------------------|
| S4 | LOW | PARTIAL. Remaining raw `requests` to fixed vendor URLs — defense-in-depth (route through `safe_http`). |
| S5 | LOW | PARTIAL. IDNA `except:pass` + thin loopback list; critical paths use `resolve=True`. |
| A6 | LOW | CONFIRM. Per-email lockout DoS + fail-open. |
| E4 | LOW | PARTIAL (was HIGH). `to_json` skips non-str; JSON not directly spreadsheet-executable. |
| B3 | LOW | PARTIAL. `attempt_count` unbounded (HMAC-gated, low control). |
| B4 | LOW | CONFIRM. Public pricing uncached `stripe.Coupon.retrieve` + bare except. |
| I4 | LOW | CONFIRM. CORS `*`+credentials if misconfigured. |
| N3 | LOW | **NEW (Codex).** Connector `gis_endpoint`/`assessor_url` validated but not persisted — dead validation. |

### REFUTED by Codex (not exploitable — no fix, optional hygiene only)
| ID | Verdict |
|----|---------|
| S3 | REFUTE. GIS fetches already use `safe_get(resolve=True)`; no validate-time-only TOCTOU. |
| A5 | REFUTE. Broad `/refresh` except is poor hygiene but RedisError→503 is caught separately. |
| T1 | REFUTE (was HIGH). Session JWT hits non-download branch requiring `aud=bridgeleads-api`; job access still `Job.user_id==user.id` — not cross-tenant. Optional: decode with explicit audience. |
| T4 | REFUTE. `download_export` sets RLS ctx before tenant reads and filters by user. |
| E2 | REFUTE (was CRITICAL). `.xlsx` writes sanitized inline strings; leading `"` is not an Excel formula start. |
| E5 | REFUTE. DNC footer is a static constant, not attacker data. |

**Tally:** 9 HIGH (8 code + 1 infra-verify) · 8 MEDIUM (2 new) · 8 LOW (1 new) · 6 refuted. Zero true Criticals after reconciliation.

## Round 3 — Remediation + mutual re-verify: _starting_

