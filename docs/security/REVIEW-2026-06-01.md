# Security Review — BridgeLeads — 2026-06-01

**Method:** Security Analyst role (`docs/security/security-analyst-agent.md`) + Master Security Review framing, run as 5 parallel domain reviewers (read-only), then cross-checked with Codex (gpt-5.5, session `019e8443`) per `.claude/rules/codex-collaboration.md`.
**Scope:** tenant isolation, scraper/enrichment SSRF, exports (CSV + R2), secrets/config/broker, auth/authz/errors/rate-limit/webhooks.
**Reviewers modified no code.**

## Tally
Critical **1** · High **9** · Medium **9** · Low **8**

## Verdict: **NO-GO** until the Critical + the 9 Highs are resolved (or explicitly risk-accepted).

---

## Deploy-blockers (Codex-confirmed)

### CRITICAL-1 — User-controlled webhook = SSRF + signed-URL exfiltration  ✅ FIXED (2026-06-01, Codex-verified PASS)
> Fix shipped: `validate_outbound_webhook()` (HTTPS-only + DNS resolution against an expanded blocked-IP set) called in the worker immediately before POST; blocked → `status:"blocked"`, no retry; `requests.Session(trust_env=False)` + `allow_redirects=False`; 3xx = non-retryable. Save-time structural HTTPS check in `DeliverConfig.webhook_url_format`. Tests: `tests/test_webhook_ssrf.py` (22).

`src/workers/webhook_delivery.py:169` POSTs to `DeliverConfig.webhook_url` (`src/api/schemas.py:121-130`), validated only by a format check (starts with `http(s)://`, ≤2000 chars) — **never `validate_scraping_target()`**. Dispatch at `src/workers/tasks.py:652-677`, and the payload embeds a 48h signed R2 download URL.
**Impact:** Any Business-plan user sets `webhook_url=http://169.254.169.254/...` or `http://localhost:6379/` and the worker fetches it from inside Railway's network — cloud IAM/metadata exfiltration, internal-service probing, plus the signed export URL is handed to an attacker endpoint. Codex: **REAL, Critical, fix first.**
**Fix:** Validate `webhook_url` through a dedicated outbound-webhook SSRF guard at config-save **and** in the worker before POST; HTTPS-only; resolve DNS + reject private/link-local/metadata IPs; disable/re-validate redirects. (Depends on HIGH-2 for the DNS piece.)

### HIGH-1 (keystone) — DNS rebinding defeats the SSRF firewall  ✅ FIXED (webhook + scraper navigation; 2a)
> Webhook path: `validate_outbound_webhook(resolve=True)`. Scraper path (2a): `navigate()`/`probe()` use `resolve=True`; `safe_goto()` added for raw-goto sites; `src/utils/safe_http.safe_get()` for server-side fetches. **Per-hop redirect interception** closed via a context-level `_ssrf_route_guard` that aborts disallowed DOCUMENT navigations *before* the request is sent (main-frame must be allowlisted; sub-frames e.g. reCAPTCHA only clear blocked-IP/DNS checks). **Live-validated with real Chromium**: allowlisted nav loads, metadata/private IPs return `ERR_BLOCKED_BY_CLIENT`. Remaining HIGH-3/4/5 (template `page.goto` wiring, detail-href cookie fetch, gis_endpoint) tracked in `tasks/todo.md` 2b–2d.

`src/api/middleware/security.py:73-131` blocks only **literal** private/metadata IPs (`ipaddress.ip_address(hostname)`); for a hostname it does **no DNS resolution** (no `getaddrinfo`/`socket` anywhere in `src/`). `requests`/Playwright resolve at connect time → TOCTOU.
**Impact:** A hostname resolving to `169.254.169.254`/RFC1918 passes validation, then the client connects to the private IP. Codex downgraded from Critical→**High** (scraper path is allowlist-constrained), but this is the keystone: CRITICAL-1's fix is bypassable until this lands.
**Fix:** Resolve the host inside `validate_scraping_target()`, reject if any A/AAAA is in a blocked range; re-check after redirects; ideally connect-time IP pinning or network-level egress block.

### HIGH-2 — Production DB role bypasses RLS (the "belt" doesn't exist)  🟡 PLANNED (audit done; migration plan ready — `docs/security/HIGH-2-rls-migration-plan.md`)
> Read-only audit (2026-06-01) + Codex design review complete. Key findings: **no table uses `FORCE ROW LEVEL SECURITY`** (so a role swap alone is a no-op if the runtime role owns the tables), and `SET LOCAL`-based RLS context **dies on commit** inside worker sessions (a latent bug FORCE would expose). Plan: two non-owner `NOBYPASSRLS` roles (`bridgeleads_app`/`bridgeleads_system`), role-based policy escape + explicit `WITH CHECK` + `after_begin` GUC reapply, route 8 worker/scheduler sites through the system session, FORCE enabled last, staging-first with extended RLS tests. **Implementation pending** — needs role provisioning + staging cutover + sign-off (not applied blind).

Deployed `postgres` role has `BYPASSRLS=true` (`tasks/progress.md:53,233`; `src/db/session.py:186-195` `check_rls_role_status`). RLS policies in migrations 001/018 are inert in prod; the **only** tenant boundary is the app-layer `WHERE user_id` filter. `tests/test_rls_isolation.py` proves the policies are correct under a `NOBYPASSRLS` role. Both reviewer and Codex: **REAL, High.**
**Fix:** Migrate the app to a `NOBYPASSRLS NOSUPERUSER` role + `FORCE ROW LEVEL SECURITY` on tenant tables. **Risky:** first audit/refactor `system_sync_session()` worker paths (tracerfy ingest, dispatcher, scheduler, daily scrape) so they don't break under the non-bypass role.

---

## Remaining High findings

### HIGH-3 — EagleWeb detail-page fetch bypasses firewall + forwards session cookies  ✅ FIXED (2b)
> `eagleweb.py` detail-href fetch now uses `safe_get(href, same_origin_as=self.base_url, require_allowlisted=True, cookies=...)` — session cookies can only go to the portal's own origin (scheme+host+port); a scraped cross-origin/blocked href raises → `_fetch_one` returns None (skipped). Codex PASS.

`src/scrapers/templates/eagleweb.py:750` `requests.get(detail_href, cookies=...)` where `detail_href` is scraped from page DOM (`:580,594`) — no `validate_scraping_target()`. Malicious/MITM'd portal emits an internal `href`; worker fetches it with session cookies attached. **Fix:** validate `href` + restrict to the connector's allowlisted host before fetch.

### HIGH-4 — `county_connectors.gis_endpoint` fetched but never validated  ✅ FIXED (2d)
> `county_gis.py` DB-`endpoint` fetches (`_query_gis`, `_query_gis_by_name`, batch) now use `safe_get(endpoint, params=..., require_allowlisted=False)` — authoritative, path-independent (covers migration-seeded connectors). Connector-create route also validates `body.gis_endpoint` (`resolve=True`) as defense. Codex PASS.

`src/scrapers/enrichment/county_gis.py:161,201,236,431,513` `requests.get(gis_endpoint)`; the connector-create route (`src/api/routes/scrapers.py:334-354`) validates `base_url` but **not** `gis_endpoint`. Stored SSRF. **Fix:** validate `gis_endpoint` in `create_connector` exactly like `base_url`.

### HIGH-5 — 8 template `page.goto()` calls skip `navigate()` (no SSRF re-validation)  ✅ FIXED (by context guard)
> Closed structurally by the context-level `_ssrf_route_guard`: every browser DOCUMENT navigation — including raw `page.goto` in templates and each redirect hop — is intercepted and validated pre-flight, so the 8 sites needed no per-file change. The one page-level route (`king_wa_probate` `**/*Search*`) was a precedence bypass — fixed to call the shared `_ssrf_nav_allowed`. Live-validated with Chromium. Codex PASS.

`eagleweb.py:156`, `acclaimweb.py:136,311`, `ava_fidlar.py:329`, `landmarkweb.py:170`, `whatcom_wa.py:137`, `king_wa_probate.py:444`, `enrichment/king_county_assessor.py:109`. The redirect-revalidation only lives in `self.navigate()`. **Fix:** route all navigation through `navigate()`; where raw `goto` is required, validate before + re-validate `page.url` after.

### HIGH-6 — `sanitize_for_csv` TAB/CR/LF prefix check is dead code  ✅ FIXED
> Now coerces non-str and quotes if **raw OR cleaned** starts with a formula prefix (`= + - @ \t \r \n`) — closes both the leading-TAB/CR/LF case and the leading-whitespace-then-formula bypass Codex caught (` \t=cmd`). 15 tests in `tests/test_csv_sanitize.py`. (Residual non-P1: BOM/zero-width lead chars — future hardening.)

`src/api/middleware/security.py:268-282`: `clean_text()` strips/replaces `\r\n` and `.strip()`s **before** the `startswith(("=","+","-","@","\t","\r"))` check, so the tab/CR/LF entries can never match (and `\n` isn't even listed). `= + - @` still caught (so High not Critical), but the function silently no-ops part of its advertised contract. **Fix:** run the prefix check on the **raw** value before `clean_text`, add `\n`.

### HIGH-7 — E2E credential files evade `.gitignore`; live password on disk
`scripts/.e2e_king_creds.json` (+ `.e2e_pierce_live.json`, `.e2e_king_job.json`) don't match the `.gitignore:117-120` patterns (which list `.e2e_creds.json` etc.) → untracked, one `git add scripts/` from being committed. Contains a **live** prod password `KingDemo!2026` (JWTs in them are expired). Not yet committed. **Fix:** broaden ignore to `scripts/.e2e_*.json`; rotate `KingDemo!2026`.

### HIGH-8 — API-key auth bypasses logout-all / revocation entirely  ✅ FIXED
> `logout_all` now clears `users.api_key_hash` (after the Redis JWT-revoke succeeds), so "log out everywhere" actually kills a leaked API key (the API-key path has no `iat` to compare to the revoke timestamp). User re-issues via `POST /api-key`. Codex PASS.

`src/api/auth.py:122-145`: the API-key path returns the user after the hash lookup, never calling `is_blacklisted`/`is_revoked_by_user_logout_all` (the JWT path does). `/auth/logout-all` has **no effect** on a leaked API key. **Fix:** make `revoke_all_for_user` also null/rotate `api_key_hash`; document key rotation as the revoke path.

### HIGH-9 — Stripe `user_message` leaked to client  ✅ FIXED
> `get_subscription` no longer returns `exc.user_message`; logs `_logger.exception` server-side and returns a generic 502. Codex PASS.

`src/api/routes/billing.py:427-431` returns `detail=f"...: {exc.user_message}"` verbatim (the `/checkout` path does this correctly). Violates the reference-id error rule. **Fix:** log + return a generic message with a reference id.

---

## Medium (9) — progress
- ✅ **Unbounded Pydantic fields** — FIXED: `Field` bounds + validators on `UserLogin`/`PasswordChange` passwords, `ScraperConfigCreate`/`ConnectorCreate` slugs+state (padding-tolerant, 2-letter), `JobCreate`, `ScheduleConfig` (hour/minute ranges), `DeliverConfig` emails(≤10)/formats, `record_types` items, `UserRegister.ref`. Codex PASS (4 rounds). `tests/test_schema_bounds.py` (14).
- ✅ **CSV non-str type-crash** — subsumed by HIGH-6 (`str()` coercion).
- ✅ **`probe()` not redirect-safe** — fixed in 2a (`probe` → `safe_get`, `allow_redirects=False`).
- ✅ **Celery broker SSL `ssl.CERT_NONE` int** — INVESTIGATED (Codex source analysis: correct for the sync kombu/redis-py broker path; only the asyncio direct client needs the `"none"` string, which it already uses). Resolved as documented, not changed — switching working prod broker config for consistency was the wrong move. Clarifying comment added.
- ✅ **Tracerfy `download_url` SSRF** — FIXED: new `safe_http.safe_get_following()` (validates initial + EVERY redirect hop with `resolve=True`, `require_https=True` blocks scheme downgrade, caps hops); `download_tracerfy_csv` uses it; signed URL no longer in error messages. Codex PASS (3 rounds).
- ✅ **Rate-limit expensive reads** — FIXED: `rate_limit(zone="general", identifier=user.id)` before the DB work in `jobs.get_results`, `scrapers.get_cached_records`, `billing.activation_funnel`. Codex PASS.
- ✅ **`R2_PUBLIC_URL` public-bucket foot-gun** — FIXED: public-URL branch now requires explicit `R2_ALLOW_PUBLIC_URLS=true` (default False) else logs + falls through to presigned. Codex PASS. (TODO: add `R2_ALLOW_PUBLIC_URLS=false` to `.env.example` — file is permission-blocked for the agent.)
- ✅ **`download_export` RLS-session ordering** — FIXED: `set_config('app.current_user_id')` now runs before BOTH the Job and Result reads (was after the Job query); `text` promoted to module import, duplicate removed. Codex PASS.
- ✅ **Presigned 48h URL in delivery** — FIXED (activates on config): delivery emails/webhooks now send a revocable app download-token link (`{API_BASE_URL}/jobs/{id}/download?token=…`, blacklist + logout-all aware) via the new dependency-light `src/api/download_tokens.mint_download_token`. Gated on `API_BASE_URL` (default ""), falls back to the presigned URL until set so delivery never breaks. Codex PASS. **TODO (you):** set `API_BASE_URL` to the public Railway API domain (and add it to `.env.example` — agent is permission-blocked there).

**All 9 Mediums addressed.**

## Medium (original list)
- **CSV sanitize type-crash/bypass** — `sanitize_for_csv` called with non-str (date) values → `re.sub` TypeError → 500 (`jobs.py:738-747`); coerce to str at top.
- **Presigned URL in email + webhook (48h)** — `tasks.py:634,659,674`; send an indirection link (revocable download token) instead of a raw 48h R2 URL.
- **`R2_PUBLIC_URL` branch** — `data_exporter.py:251-252` returns a permanent public object URL with no expiry; gate behind explicit flag + refuse in production, confirm bucket public-access disabled.
- **Tracerfy `download_url`** unvalidated fetch (`skip_trace.py:461`) — validate before fetch.
- **`probe()` not redirect-safe** (`base_scraper.py:237-239`) — `allow_redirects=False` + re-validate.
- **Celery broker SSL uses `ssl.CERT_NONE` int** (`src/workers/__init__.py:24`) — project rule requires string `"none"`; route through `redis_kwargs()`. Fragile across kombu/redis bumps.
- **Expensive reads unrate-limited** — `/jobs/{id}/results`, `/scrapers/{id}/records`, `/billing/activation-funnel`; add limiter before DB work.
- **`download_export` uses non-RLS session, sets context after the ownership query** (`jobs.py:576-719`) — move `set_config` before the Job query.
- **Unbounded Pydantic fields** (`schemas.py`) — `UserLogin.password` (bcrypt CPU DoS), `ScraperConfigCreate`/`ConnectorCreate` slugs, `scraper_config_id` not UUID; add `max_length`/types.

## Low (8)
R2 objects never expire (no lifecycle rule); object keys logged (keys not URLs — keep it that way); subdomain `endswith` allowlist matching; admin routes 403 (enumerable) vs 404; no global exception handler (not a leak at `DEBUG=False`, but one bad deploy from tracebacks); two unscoped-but-safe `ScraperConfig` queries (`jobs.py:46-48`, `:220-223`); page-body PII logged at INFO in scraper templates; no central log-redaction filter.

---

## Confirmed solid (no action)
JWT alg pinned to `["HS256"]` + aud/iss/exp verified; logout-all revocation **fails closed** (503 on Redis error, durable `users.revoked_at` fallback, "cached 0 untrusted" correct); Stripe webhook verifies HMAC before mutation + idempotent + customer-match anti-tamper; tenant IDOR closed on the hot paths (jobs/scrapers/results filter by `user_id`); Celery worker re-verifies job ownership (doesn't trust `job_id`); in-app download uses a 60s job+user-scoped JWT and streams server-side; no pickle serializer (json only); no real secret in tracked code; `SECRET_KEY` ≥32 validator; CORS is an explicit allowlist (no `*`+credentials); `sanitize_for_csv` is applied on every export path (the defect is internal to the function).

## Recommended fix order
1. **CRITICAL-1 webhook SSRF** + **HIGH-1 DNS rebinding** together (one without the other is incomplete).
2. **HIGH-7** gitignore + rotate `KingDemo!2026` (cheap, prevents a credential entering history).
3. **HIGH-3/4/5** scraper SSRF bypass cluster (shared fix: everything through validation).
4. **HIGH-8** API-key revocation, **HIGH-9** Stripe leak, **HIGH-6** CSV sanitizer (all small).
5. **HIGH-2 RLS role** (highest-effort; needs worker-path audit first).
6. Mediums, then Lows.
